# Extend with Logger

TODO

<!-- ```ts
  logger: {
    level: 'debug',
    log: (level: string, message: string, ...args: any[]) => {
      console.log(`[${level}] ${message} ${JSON.stringify(args)}`)
    },
  },
``` -->
