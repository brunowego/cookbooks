# Extend with onApiError

TODO

<!-- ```ts
import type { AuthContext } from 'better-auth'

onAPIError: {
  throw: true,
  onError: (error: unknown, ctx: AuthContext) => {
    console.error('Auth API error', error, ctx)
  },
},
``` -->
