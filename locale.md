# Locale

## Commands

```sh
locale -a
```

## Environment

For Bash or Zsh, put something like this in your `$HOME/.bashrc` or `$HOME/.zshrc`:

```sh
# Locale
export LANG=en_US.UTF-8
```

```sh
sudo su - $USER
```
