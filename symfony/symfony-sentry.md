# Symfony Sentry

TODO

<!--
sentry:
  dsn: '%env(SENTRY_DSN)%'
  messenger:
    enabled: true
    capture_soft_fails: true
  options:
    release: '%env(APP_VERSION)%'
    environment: '%env(SENTRY_ENV)%'
-->
