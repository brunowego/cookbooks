# GNU Autoconf

## CLI

### Installation

#### Homebrew

```sh
brew install autoconf
```

#### APT

```sh
sudo apt update
sudo apt -y install autoconf
```

#### YUM

```sh
yum check-update
sudo yum -y install autoconf
```
